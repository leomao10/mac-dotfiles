name: Leo Liang's Organization CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    

jobs:
  caling-reuse-workflow-in-dot-github:
    uses: leomao10/.github/workflows/reusable-workflow.yml@master
